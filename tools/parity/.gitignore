# Run output. Each run is a dated artifact of a moment; a session attaches
# the composite and table it needs to its report, and the rest is noise in
# a diff. The harness itself is what's committed.
out/
